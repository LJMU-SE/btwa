import Layout from "@/components/Layout/Layout";

function NotFound() {
    return (
        <Layout title={"Capturing Video"} links={true} navbar={true}>
            <h1>Not Found</h1>
        </Layout>
    );
}

export default NotFound;
